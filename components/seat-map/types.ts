/**
 * @deprecated This file is maintained for backwards compatibility.
 * Please import types from '@/types' instead.
 *
 * Example:
 * import { Seat, Row, Zone } from '@/types';
 */

// Re-export all types from centralized types directory
export type {
  Seat,
  Row,
  Area,
  Zone,
  Category,
  Venue,
  MapData,
  ViewerState,
  LegendItem,
  SeatStatus,
  GuestForm,
} from '@/types';
